package cintlex.oldlighter;

import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;

@Mod(value = OldLighter.MOD_ID, dist = Dist.CLIENT)
public class OldLighter {
    public static final String MOD_ID = "oldlighter";

    public OldLighter(IEventBus modEventBus) {
    }
}